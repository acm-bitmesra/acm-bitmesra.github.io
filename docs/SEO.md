# SEO

The app provides global metadata, canonical URLs, OpenGraph/Twitter metadata, a sitemap, robots.txt, semantic headings, internal links, clean route names, and a GitHub Pages base path.

When adding new pages, update `public/sitemap.xml` and ensure the page has a clear title, description, and one primary H1.
